import { APP_MAX_WIDTH_PX } from "@/constants";

export function Footer() {
  return (
    <footer className="bg-gray-100 border-t border-gray-200 asbolutr bottom-0 left-0 w-screen">
      <div className={`max-w-app mx-auto px-4 sm:px-6 lg:px-8`}>
        <div className="py-8"></div>
      </div>
    </footer>
  );
}
